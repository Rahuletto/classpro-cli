{
  "name": "classpro",
  "module": "dist/index.js",
  "version": "0.1.7",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "start": "bun run dist/index.js",
    "prepublish": "bun run build"
  },
  "description": "A CLI tool to initialize ClassPro front-end and back-end so it can be easily setup and hosted right away.",
  "bin": {
    "classpro": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/prompts": "^2.4.9"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.48.1",
    "colorette": "^2.0.20",
    "dotenv": "^16.4.7",
    "prompts": "^2.4.2",
    "tsx": "^4.19.2"
  }
}